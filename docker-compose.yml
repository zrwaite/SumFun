services:
    server:
        env_file: './server/.env'
        build: './'
        image: 'hargunm/freyhacks:1.0.0'
        ports:
            - '8006:8006'
# checking if workflow is restricted to changes in this file